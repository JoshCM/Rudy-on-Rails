<v:CanvasUserControl x:Class="RoRClient.Views.Editor.PublictrainstationEditorUserControl"
                     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                     xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                     xmlns:v="clr-namespace:RoRClient.Views"
                     xmlns:converter="clr-namespace:RoRClient.BindingConverter"
                     x:Name="publictrainstationEditorUserControl"
                     mc:Ignorable="d" 
                     Canvas.Top="{Binding RealY, ElementName=publictrainstationEditorUserControl}"
                     Canvas.Left="{Binding RealX, ElementName=publictrainstationEditorUserControl}">
    <UserControl.Resources>
        <converter:CompassToAngleConverter  x:Key="CompassToAngleConverter"/>
    </UserControl.Resources>
	<Canvas Width="{Binding SquareDim, ElementName=publictrainstationEditorUserControl}"
            Height="{Binding SquareDim, ElementName=publictrainstationEditorUserControl}">
		<Image Width="{Binding SquareDim, ElementName=publictrainstationEditorUserControl}" Height="{Binding SquareDim, ElementName=publictrainstationEditorUserControl}" Source="pack://application:,,,/Resources/Images/trainstation_0.png">
            <Image.InputBindings>
                <MouseBinding MouseAction="RightClick" Command="{Binding SelectInteractiveGameObjectCommand}"></MouseBinding>
            </Image.InputBindings>
            <Image.LayoutTransform>
                <TransformGroup>
                    <RotateTransform Angle="{Binding Publictrainstation.Alignment, Mode=OneWay, Converter={StaticResource CompassToAngleConverter}}" />
                </TransformGroup>
            </Image.LayoutTransform>
        </Image>
    </Canvas>
</v:CanvasUserControl>
