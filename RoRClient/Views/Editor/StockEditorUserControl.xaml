<v:CanvasUserControl x:Class="RoRClient.Views.Editor.StockEditorUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:v="clr-namespace:RoRClient.Views"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RoRClient.Views.Editor"
             xmlns:converter="clr-namespace:RoRClient.BindingConverter"
             x:Name="stockEditorUserControl"
             mc:Ignorable="d" 
             Canvas.Top="{Binding RealY, ElementName=stockEditorUserControl}"
             Canvas.Left="{Binding RealX, ElementName=stockEditorUserControl}">
    <UserControl.Resources>
        <converter:CompassToAngleConverter  x:Key="CompassToAngleConverter"/>
    </UserControl.Resources>
    <Canvas Width="{Binding SquareDim, ElementName=stockEditorUserControl}"
            Height="{Binding SquareDim, ElementName=stockEditorUserControl}">
        <Image Width="{Binding SquareDim, ElementName=stockEditorUserControl}" Height="{Binding SquareDim, ElementName=stockEditorUserControl}" Source="pack://application:,,,/Resources/Images/stock.png">
            <Image.LayoutTransform>
                <TransformGroup>
                    <RotateTransform Angle="{Binding Stock.Alignment, Mode=OneWay, Converter={StaticResource CompassToAngleConverter}}" />
                </TransformGroup>
            </Image.LayoutTransform>
        </Image>
    </Canvas>
</v:CanvasUserControl>
