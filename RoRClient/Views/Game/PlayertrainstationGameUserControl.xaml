<v:CanvasUserControl x:Class="RoRClient.Views.Game.PlayertrainstationGameUserControl"
                     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                     xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                     xmlns:v="clr-namespace:RoRClient.Views"
                     xmlns:converter="clr-namespace:RoRClient.BindingConverter"
                     x:Name="playertrainstationGameUserControl"
                     mc:Ignorable="d" 
                     Canvas.Top="{Binding RealY, ElementName=playertrainstationGameUserControl}"
                     Canvas.Left="{Binding RealX, ElementName=playertrainstationGameUserControl}">
    <UserControl.Resources>
        <converter:CompassToAngleConverter  x:Key="TrainstationAlignmentToAngleConverter"/>
    </UserControl.Resources>
    <Canvas Width="{Binding SquareDim, ElementName=playertrainstationGameUserControl}"
            Height="{Binding SquareDim, ElementName=playertrainstationGameUserControl}">
        <Image Width="{Binding SquareDim, ElementName=playertrainstationGameUserControl}" Height="{Binding SquareDim, ElementName=playertrainstationGameUserControl}" Source="pack://application:,,,/Resources/Images/trainstation.png">
            <Image.InputBindings>
                <MouseBinding MouseAction="LeftClick" Command="{Binding SelectedTrainstationObjectCommand}"></MouseBinding>
            </Image.InputBindings>
            <Image.LayoutTransform>
                <TransformGroup>
                    <RotateTransform Angle="{Binding Playertrainstation.Alignment, Mode=OneWay, Converter={StaticResource TrainstationAlignmentToAngleConverter}}" />
                </TransformGroup>
            </Image.LayoutTransform>
        </Image>
    </Canvas>
</v:CanvasUserControl>